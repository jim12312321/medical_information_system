def response_generate(result, source, source_text, responseMedia):
    d = {}
    d['result'] = result
    d['source'] = source
    d['sourceText'] = source_text
    d['responseMedia'] = responseMedia

    return d